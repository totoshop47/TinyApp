<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>

<body>
  <header>
    <% include ./partials/_header %>
  </header>
  <ul>
    <% for(var shortURL in urls) { %>
    <li><a href= <%= urls[shortURL] %>><%= `${shortURL}` %></a> <%= `, ${urls[shortURL]}` %></li>
    <% } %>
  </ul>

</body>
</html>

